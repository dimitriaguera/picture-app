

const initialState = {

}

function matrixReducer(state = initialState, action) {
    let nextState;
    switch (action.type) {
        default:
            return state;
    }
    return nextState;
}

export default matrixReducer;